# Mutator Audit

The following 8 daily-tick-requiring mutators are NOT currently active for any user and have not yet been implemented server-side.

Do not enable these mutators for users until the corresponding server-side logic is built into `daily_mutator_tick.py`.

1. `lazy_bones`
2. `procrastinator_tax`
3. `sleep_deprivation`
4. `perfectionist_curse`
5. `distraction_demon`
6. `brain_fog`
7. `burnout_fever`
8. `comfort_zone_trap`

*(Note: If a mutator isn't actively used, retiring the legacy engine is a good time to reconsider if it should exist at all.)*
